import styles from './LoginPage.module.css';

export function LoginPage() {
  return (
    <div className={styles['canvas-page']}>
      <h2 className={styles['login-title']}>LOGIN page coming soon”</h2>
    </div>
  );
}
